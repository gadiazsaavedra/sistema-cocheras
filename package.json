{
  "name": "sistema-cocheras",
  "version": "1.0.0",
  "description": "Sistema de gestión de cocheras con control anti-fraude",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run build:client",
    "build:client": "cd client && npm run build",
    "migrate": "node migrations/run-migrations.js",
    "backup": "node -e \"const admin=require('firebase-admin');const BackupSystem=require('./backup-system');const serviceAccount=require('./firebase-service-account.json');admin.initializeApp({credential:admin.credential.cert(serviceAccount)});(async()=>{const backup=new BackupSystem();await backup.createBackup();process.exit(0);})();\"",
    "setup-empleados": "node setup-empleados.js",
    "deploy": "./deploy.sh"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^11.11.0",
    "moment": "^2.29.4",
    "multer": "^1.4.4",
    "node-cron": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
